<style global>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  /* :global(html, body) {
    height: 100vh;
    width: 100vw;
    padding: 0;
    margin: 0;
  }

  :global(h1) {
    font-family: "Montserrat", sans-serif;
    font-weight: 900;
  }

  :global(font-title) {
    font-family: "Montserrat", sans-serif;
    font-weight: 900;
  }

  :global(p) {
    @apply pb-2;
  }

  :global(a) {
    @apply text-green-400;
  }

  :global(*:focus) {
    outline: none;
  } */
</style>
